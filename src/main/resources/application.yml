spring:
  application:
    name: myPets

  # 默认使用 H2 内存数据库（开发环境）
  datasource:
    url: jdbc:h2:mem:mypets
    driver-class-name: org.h2.Driver
    username: sa
    password:

  # 启用 H2 Console（开发调试用）
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080
