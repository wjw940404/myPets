spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mypets?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: ${MYSQL_PASSWORD}

  h2:
    console:
      enabled: false

server:
  port: 8080
